name: Deploy to Server

on:
  push:
    branches: [main]

env:
  DOCKER_BUILD_RECORD: "true"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: |
          docker build \
            --build-arg NEXT_PUBLIC_CONVEX_URL=${{ secrets.CONVEX_URL }} \
            --build-arg NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=${{ secrets.CLERK_PUBLISHABLE_KEY }} \
            --build-arg NEXT_PUBLIC_COLLABORATION_URL=${{ secrets.COLLABORATION_URL }} \
            --build-arg CLERK_SECRET_KEY=${{ secrets.CLERK_SECRET_KEY }} \
            --build-arg CLERK_ISSUER_DOMAIN=${{ secrets.CLERK_ISSUER_DOMAIN }} \
            -t docs-collaborate .

      - name: Save image
        run: docker save docs-collaborate | gzip > image.tar.gz

      - name: Copy files to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          source: "image.tar.gz,docker-compose.yml"
          target: "~/docs-collaborate"

      - name: Deploy on server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd ~/docs-collaborate
            cat > .env.production << 'EOF'
            NEXT_PUBLIC_CONVEX_URL=${{ secrets.CONVEX_URL }}
            NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=${{ secrets.CLERK_PUBLISHABLE_KEY }}
            NEXT_PUBLIC_COLLABORATION_URL=${{ secrets.COLLABORATION_URL }}
            CLERK_SECRET_KEY=${{ secrets.CLERK_SECRET_KEY }}
            CLERK_ISSUER_DOMAIN=${{ secrets.CLERK_ISSUER_DOMAIN }}
            CONVEX_SITE_URL=${{ secrets.CONVEX_SITE_URL }}
            COLLABORATION_SECRET=${{ secrets.COLLABORATION_SECRET }}
            COLLABORATION_ALLOWED_ORIGINS=${{ secrets.COLLABORATION_ALLOWED_ORIGINS }}
            EOF
            docker load < image.tar.gz
            docker compose up -d --force-recreate
            rm -f image.tar.gz
