services:
  app:
    build: .
    container_name: docs-collaborate
    ports:
      - "3000:3000"
    env_file:
      - .env.production
    environment:
      - NODE_ENV=production
    restart: unless-stopped
